---
import { corporateNav, orocafeNav } from '~/lib/site';
import Monogram from './Monogram.astro';

interface Props {
  brand: 'discovery' | 'orocafe';
}

const { brand } = Astro.props;
const nav = brand === 'orocafe' ? orocafeNav : corporateNav;
const pathname = Astro.url.pathname;
---

<header class="sticky top-0 z-40 backdrop-blur-xl" style="background: color-mix(in srgb, var(--color-paper) 80%, transparent); border-bottom: 1px solid color-mix(in srgb, var(--color-line) 50%, transparent);">
  <div class="mx-auto max-w-[var(--container-7xl)] px-6 lg:px-12">
    <div class="flex items-center justify-between gap-6 h-16 lg:h-[72px]">

      {/* Logo cluster */}
      <a href={brand === 'orocafe' ? '/orocafe' : '/'} class="flex items-center gap-2.5 group" aria-label={brand === 'orocafe' ? 'Orocafe home' : 'Discovery Group home'}>
        {brand === 'orocafe' ? (
          <span style="font-weight: 600; font-size: 1.25rem; letter-spacing: -0.025em; color: var(--color-oro);">Orocafe</span>
        ) : (
          <>
            <Monogram class="text-[var(--color-clay)]" size={28} />
            <div class="flex flex-col leading-none">
              <span style="font-weight: 600; font-size: 0.9375rem; letter-spacing: -0.015em;">Discovery</span>
              <span class="mt-0.5 text-[0.625rem] uppercase tracking-[0.22em]" style="color: var(--color-ink-muted); font-weight: 500;">Group · Uganda</span>
            </div>
          </>
        )}
      </a>

      {/* Centre nav */}
      <nav class="hidden lg:flex items-center gap-8" aria-label="Primary">
        {nav.map((item) => {
          const isActive = pathname === item.href || (item.href !== '/' && item.href !== '/orocafe' && pathname.startsWith(item.href));
          return (
            <a
              href={item.href}
              class:list={[
                'text-sm transition-colors py-1 relative',
                isActive ? 'text-[var(--color-ink)]' : 'text-[var(--color-ink-muted)] hover:text-[var(--color-ink)]'
              ]}
              aria-current={isActive ? 'page' : undefined}
              style={isActive ? "font-weight: 500;" : ""}
            >
              {item.label}
            </a>
          );
        })}
      </nav>

      {/* Right cluster */}
      <div class="flex items-center gap-3 lg:gap-4">
        <div class="hidden sm:flex items-center text-[0.625rem] tracking-[0.22em] uppercase font-medium">
          <a href="/"
            class:list={['px-2.5 py-1 transition-opacity',
              brand === 'discovery' ? 'text-[var(--color-ink)]' : 'text-[var(--color-ink-muted)] hover:opacity-70']}
            aria-current={brand === 'discovery' ? 'page' : undefined}
          >DG</a>
          <span class="text-[var(--color-line)]" aria-hidden="true">·</span>
          <a href="/orocafe"
            class:list={['px-2.5 py-1 transition-opacity',
              brand === 'orocafe' ? 'text-[var(--color-ink)]' : 'text-[var(--color-ink-muted)] hover:opacity-70']}
            aria-current={brand === 'orocafe' ? 'page' : undefined}
          >Orocafe</a>
        </div>

        {brand === 'discovery' && (
          <a href="/contact" class="hidden md:inline-flex items-center gap-1.5 text-sm font-medium px-4 py-2 bg-[var(--color-ink)] text-[var(--color-paper)] hover:bg-[var(--color-clay)] transition-colors rounded-full" style="height: 36px;">
            Inquire
            <span aria-hidden="true">→</span>
          </a>
        )}

        <details class="lg:hidden relative">
          <summary class="list-none cursor-pointer p-2 -mr-2" aria-label="Open menu">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M3 6h18M3 12h18M3 18h18" />
            </svg>
          </summary>
          <div class="absolute right-0 top-full mt-2 border border-[var(--color-line)] bg-[var(--color-paper)] py-2 min-w-[16rem] shadow-2xl rounded-lg">
            {nav.map((item) => (
              <a href={item.href} class="block px-5 py-2.5 text-sm hover:bg-[var(--color-paper-warm)]">{item.label}</a>
            ))}
            <div class="border-t border-[var(--color-line-soft)] mt-2 pt-2">
              <a href="/" class="block px-5 py-2 text-xs uppercase tracking-wider text-[var(--color-ink-muted)] hover:bg-[var(--color-paper-warm)]">Discovery Group</a>
              <a href="/orocafe" class="block px-5 py-2 text-xs uppercase tracking-wider text-[var(--color-ink-muted)] hover:bg-[var(--color-paper-warm)]">Orocafe</a>
            </div>
          </div>
        </details>
      </div>
    </div>
  </div>
</header>
